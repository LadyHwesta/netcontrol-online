fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36   # [asyncio] extra pulls in greenlet -- required by async SQLAlchemy's
                               # sync-to-async bridging (greenlet_spawn), missing without it
psycopg2-binary==2.9.10   # only for migrate.py/gmrs_sync.py, which use raw psycopg2 directly
asyncpg==0.31.0           # async driver for the app's own DB access (database.py)
PyJWT==2.10.1
bcrypt==4.2.0
python-multipart==0.0.19
pydantic[email]==2.10.3
python-dotenv==1.0.1
httpx==0.28.1
# Was already present transitively (pulled in by pywebpush below), but is now
# imported directly too -- activitypub_signing.py's RSA keypair generation
# and HTTP Signature signing/verification for Fediverse participation.
cryptography==50.0.1
# Real point-in-polygon geometry (issue #28's incident-affected-station
# matching against real evacuation zone polygons from issue #27) -- a core
# dependency, not lazily imported like the optional integrations below:
# GeoJSON Polygon/MultiPolygon geometry from real government sources can
# have interior holes and multiple rings, and this result decides who gets
# contacted about a possible evacuation, so hand-rolling the algorithm was
# judged the wrong call here. Pulls in numpy.
shapely==2.1.2
slowapi==0.1.9
# Only needed if you set CAPTCHA_PROVIDER=altcha — safe to remove this line
# if you're using turnstile/recaptcha or no bot protection at all; the app
# imports it lazily and only when that code path actually runs.
altcha==2.1.0

# Only needed if you set TRANSLATION_ENABLED=true — safe to remove these two
# lines otherwise; the app imports argostranslate lazily and only when that
# code path actually runs. This is a genuinely heavy dependency chain (it
# pulls in ctranslate2, spacy, and stanza) next to everything else in this
# file -- see README's "UI Translation" section before enabling on a small
# or resource-constrained server.
argostranslate==1.11.0

# Only needed if you set REDIS_URL (running uvicorn with WORKERS > 1) --
# safe to remove otherwise; the rate limiter and DMR/APRS relay-push caches
# fall back to in-memory storage (correct only for a single worker process,
# today's default) when REDIS_URL is unset, and this is only ever imported
# lazily, when that env var is actually set. See README's "Running with
# multiple workers" section.
redis==5.2.1

# Only needed if you set VAPID_PUBLIC_KEY/VAPID_PRIVATE_KEY -- safe to
# remove otherwise; the app imports pywebpush lazily and only when that
# code path actually runs (sending a web push notification). See README's
# "Web Push Notifications" section.
pywebpush==2.5.0
